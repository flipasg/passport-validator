# Agent Guidelines

- Run `npm test` before completing any task that touches source code or tests.
- Keep domain logic inside `src/domain` and expose UI-ready state through composables in `src/application`.
- Style interactive components with the Tailwind theme defined in `tailwind.config.ts`.
